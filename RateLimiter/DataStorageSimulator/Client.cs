namespace RateLimiter.DataStorageSimulator
{
    //simulates the list of clients that might be sending requests
    public enum Client
    {
        UnRated = 0,
        AClient = 1,
        BClient = 2,
        CClient = 3
    }
}
